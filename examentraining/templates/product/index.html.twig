{% extends 'base.html.twig' %}
{% block body %}
<div class="container">
    <div class="row">
        {% for movie in movies %}
            <div class="col-md-3">
                <h3>{{ movie.name }}</h3>
{#                <img src="{{ movie.image }}" alt="image">#}
                <h5 class="mt-4">{{ movie.description }}</h5>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
